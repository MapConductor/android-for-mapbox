<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
            Mapbox public access token for map rendering.
            Add the following to your ~/.gradle/gradle.properties (do NOT commit to version control):
              MAPBOX_DOWNLOADS_TOKEN=sk.eyJ1...  (for SDK download)
            For the public token used at runtime, set it programmatically via:
              MapboxOptions.accessToken = "pk.eyJ1..."
            or add it here:
              <meta-data android:name="com.mapbox.token" android:value="pk.eyJ1..." />
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
